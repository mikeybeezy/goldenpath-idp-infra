apiVersion: v1
kind: Secret
metadata:
 name: apikey             # Must match name referenced in KongConsumer
 namespace: app-namespace
 labels:
   konghq.com/credential: key-auth
type: Opaque
stringData:
 kongCredType: key-auth
 key: secret-key               # Your chosen API key value

