# Managed by scripts/standardize_metadata.py
apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: guestbook
    tier: frontend
  annotations:
    goldenpath.idp/id: APPS_SAMPLE_STATELESS_APP_METADATA
    goldenpath.idp/owner: platform-team
spec:
  type: LoadBalancer
  ports:
    - port: 80
  selector:
    app: guestbook
    tier: frontend
