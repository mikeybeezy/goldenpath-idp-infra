apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ values.app_name }}-dashboard"
  namespace: "{{ values.namespace }}"
  labels:
    grafana_dashboard: "1"
data:
  {{ values.app_name }}-dashboard.json: |
    {
      "title": "{{ values.app_name }} Overview",
      "tags": ["{{ values.team }}", "{{ values.env }}"]
    }
