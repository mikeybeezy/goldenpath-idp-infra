apiVersion: v1
kind: ConfigMap
metadata:
  name: "<APP_NAME>-dashboard"
  namespace: "<NAMESPACE>"
  labels:
    grafana_dashboard: "1"
data:
  <APP_NAME>-dashboard.json: |
    {
      "title": "<APP_NAME> Overview",
      "tags": ["<TEAM>", "<ENV>"]
    }
