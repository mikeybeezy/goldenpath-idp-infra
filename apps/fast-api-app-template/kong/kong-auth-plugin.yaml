apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: "<KONG_AUTH_PLUGIN>"
  namespace: "<NAMESPACE>"
  annotations:
    kubernetes.io/ingress.class: kong
plugin: key-auth
config:
  key_names:
    - apikey
  hide_credentials: true
