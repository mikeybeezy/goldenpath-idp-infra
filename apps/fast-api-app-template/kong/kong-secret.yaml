apiVersion: v1
kind: Secret
metadata:
  name: "{{ values.kong_credential_secret_name }}"
  namespace: "{{ values.namespace }}"
  labels:
    konghq.com/credential: key-auth
type: Opaque
stringData:
  kongCredType: key-auth
  key: "{{ values.kong_api_key }}"
