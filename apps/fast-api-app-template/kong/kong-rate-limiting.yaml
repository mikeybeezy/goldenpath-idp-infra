apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: "<KONG_RATE_LIMIT_PLUGIN>"
  namespace: "<NAMESPACE>"
  annotations:
    kubernetes.io/ingress.class: kong
plugin: rate-limiting
config:
  minute: <RATE_LIMIT_PER_MIN>
  limit_by: consumer
  policy: local
