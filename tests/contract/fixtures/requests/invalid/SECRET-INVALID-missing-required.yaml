# Invalid SecretRequest - missing required fields
# Used for contract testing error handling
apiVersion: goldenpath.idp/v1
kind: SecretRequest
metadata:
  id: SECRET-INVALID
  # Missing: name, service, environment, owner
spec:
  secretType: database
  # Missing: riskTier, rotationClass, lifecycleStatus, namespace, k8sSecretName
