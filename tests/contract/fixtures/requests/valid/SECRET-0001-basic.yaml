# Valid SecretRequest fixture for contract testing
# Represents the minimum viable secret request
apiVersion: goldenpath.idp/v1
kind: SecretRequest
metadata:
  id: SECRET-0001
  name: example-database-credentials
  service: backstage
  environment: dev
  owner: platform-team
spec:
  secretType: database
  riskTier: high
  rotationClass: quarterly
  lifecycleStatus: active
  namespace: backstage
  k8sSecretName: backstage-db-credentials
  provider: aws-secrets-manager
