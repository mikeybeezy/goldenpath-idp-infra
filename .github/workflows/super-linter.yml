# Owner: platform
name: Quality - Super Linter (Markdown)

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  super-linter:
    name: Quality - Super Linter (Markdown)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      statuses: write
    steps:
      - name: Super Linter Check (Disabled)
        run: echo "Super Linter is strictly disabled via workflow config. Passing."
      - name: Runbook link
        if: always()
        run: |
          echo "Runbook: docs/90-doc-system/30_DOCUMENTATION_FRESHNESS.md" >> "${GITHUB_STEP_SUMMARY}"
