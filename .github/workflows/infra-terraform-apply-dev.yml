name: Infra Terraform Apply (dev)

on:
  workflow_dispatch:
    inputs:
      confirm_apply:
        description: "Type 'apply' to confirm dev apply"
        required: true
        default: "cancel"
        type: choice
        options:
          - cancel
          - apply
      build_id:
        description: "Build ID (dd-mm-yy-NN)"
        required: true
        type: string

jobs:
  apply-dev:
    if: ${{ inputs.confirm_apply == 'apply' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      actions: read
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Validate build ID
        shell: bash
        run: |
          if ! [[ "${{ inputs.build_id }}" =~ ^[0-9]{2}-[0-9]{2}-[0-9]{2}-[0-9]{2}$ ]]; then
            echo "Invalid build_id: '${{ inputs.build_id }}' (expected dd-mm-yy-NN)"
            exit 1
          fi

      - name: Verify dev plan succeeded for this commit
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const workflowId = "infra-terraform.yml";
            const sha = context.sha;
            const runs = await github.rest.actions.listWorkflowRuns({
              owner,
              repo,
              workflow_id: workflowId,
              event: "workflow_dispatch",
              per_page: 20
            });
            const match = runs.data.workflow_runs.find(
              run =>
                run.conclusion === "success" &&
                run.head_sha === sha &&
                typeof run.display_title === "string" &&
                run.display_title.includes("env=dev")
            );
            if (!match) {
              core.setFailed(
                `No successful dev plan run found for ${sha}. Run infra-terraform.yml with env=dev first.`
              );
            } else {
              core.info(`Dev plan run ok: ${match.html_url}`);
            }

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Configure AWS credentials using OIDC (apply role)
        uses: aws-actions/configure-aws-credentials@v4.1.0
        with:
          aws-region: eu-west-2
          role-to-assume: ${{ secrets.TF_AWS_IAM_ROLE_DEV_APPLY }}

      - name: Terraform init (backend)
        run: |
          terraform -chdir=envs/dev init \
            -backend-config="bucket=goldenpath-idp-dev-bucket" \
            -backend-config="key=envs/dev/terraform.tfstate" \
            -backend-config="region=eu-west-2" \
            -backend-config="dynamodb_table=goldenpath-idp-dev-db-key" \
            -backend-config="encrypt=true"

      - name: Terraform apply (dev)
        env:
          TF_VAR_build_id: ${{ inputs.build_id }}
        run: terraform -chdir=envs/dev apply -auto-approve -input=false -no-color
