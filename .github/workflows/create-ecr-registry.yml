name: Create ECR Registry

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        type: choice
        required: true
        options: [dev, test, staging, prod]
      registry_name:
        description: 'Registry name (e.g., wordpress-platform)'
        required: true
      owner:
        description: 'Team owner (e.g., app-team-wordpress)'
        required: true
      risk:
        description: 'Risk level'
        type: choice
        required: true
        options: [low, medium, high]

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  propose:
    name: Propose Registry Creation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Calculate Registry ID
        id: calc
        run: |
          # Convert wordpress-platform to REGISTRY_WORDPRESS_PLATFORM
          NAME="${{ inputs.registry_name }}"
          ID=$(echo "$NAME" | tr '-' '_' | tr '[:lower:]' '[:upper:]' | sed 's/^/REGISTRY_/')
          echo "id=$ID" >> $GITHUB_OUTPUT
          echo "✅ Calculated ID: $ID"

      - name: Install yq
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq

      - name: Validate inputs
        run: |
          # Validate registry name format
          if ! echo "${{ inputs.registry_name }}" | grep -qE '^[a-z][a-z0-9-]*$'; then
            echo "❌ Registry name must be lowercase with hyphens only"
            exit 1
          fi

          # Validate owner format (relaxed)
          if ! echo "${{ inputs.owner }}" | grep -qE '^[a-z0-9-]+$'; then
            echo "❌ Owner must be lowercase alphanumeric with dashes (e.g. platform-team, michael-babs)"
            exit 1
          fi

          echo "✅ All inputs validated"

      - name: Update catalog (idempotent)
        run: |
          set -euo pipefail
          FILE="docs/catalogs/ecr-catalog.yaml"
          NAME="${{ inputs.registry_name }}"
          OWNER="${{ inputs.owner }}"
          RISK="${{ inputs.risk }}"
          ID="${{ steps.calc.outputs.id }}"
          ENV="${{ inputs.environment }}"

          # Ensure file exists with proper structure
          if [ ! -f "$FILE" ]; then
            cat > "$FILE" <<EOF
          version: "1.0"
          domain: "container-registries"
          owner: "platform-team"
          last_updated: "$(date -u +%Y-%m-%d)"
          managed_by: "platform-team"
          registries: {}
          EOF
          fi

          # Add/update the registry entry
          yq -i \
            ".registries.\"$NAME\".metadata.id = \"$ID\" |
             .registries.\"$NAME\".metadata.owner = \"$OWNER\" |
             .registries.\"$NAME\".metadata.risk = \"$RISK\" |
             .registries.\"$NAME\".metadata.status = \"pending\" |
             .registries.\"$NAME\".metadata.created_date = \"$(date -u +%Y-%m-%d)\" |
             .last_updated = \"$(date -u +%Y-%m-%d)\"" \
            "$FILE"

          echo "✅ Updated catalog for registry: $NAME (ID: $ID)"

      - name: Update Terraform tfvars
        env:
          INPUT_ENVIRONMENT: ${{ inputs.environment }}
          INPUT_REGISTRY_NAME: ${{ inputs.registry_name }}
          INPUT_OWNER: ${{ inputs.owner }}
          INPUT_RISK: ${{ inputs.risk }}
          INPUT_ID: ${{ steps.calc.outputs.id }}
        run: |
          python3 -c '
          import os
          import sys

          env = os.environ.get("INPUT_ENVIRONMENT")
          tfvars_path = "envs/" + env + "/terraform.tfvars"
          name = os.environ.get("INPUT_REGISTRY_NAME")
          owner = os.environ.get("INPUT_OWNER")
          risk = os.environ.get("INPUT_RISK")
          reg_id = os.environ.get("INPUT_ID")

          # HCL Entry to insert
          new_entry = f"""
            "{name}" = {{
              metadata = {{
                id    = "{reg_id}"
                owner = "{owner}"
                risk  = "{risk}"
              }}
            }}
          """

          try:
              with open(tfvars_path, "r") as f:
                  content = f.read()
              
              if f"\"{name}\"" in content and "ecr_repositories" in content:
                  print(f"⚠️  Registry {name} appears to already exist in {tfvars_path}")
                  sys.exit(0)

              if "ecr_repositories = {" in content:
                  start_idx = content.find("ecr_repositories = {")
                  brace_count = 0
                  insert_idx = -1
                  
                  for i in range(start_idx, len(content)):
                      if content[i] == "{":
                          brace_count += 1
                      elif content[i] == "}":
                          brace_count -= 1
                          if brace_count == 0:
                              insert_idx = i
                              break
                  
                  if insert_idx != -1:
                      new_content = content[:insert_idx] + new_entry + content[insert_idx:]
                      with open(tfvars_path, "w") as f:
                          f.write(new_content)
                      print(f"✅ Injected {name} into existing ecr_repositories block.")
                  else:
                      print("❌ Error: Could not parse ecr_repositories block structure.")
                      sys.exit(1)
              else:
                  with open(tfvars_path, "a") as f:
                      f.write(f"\n# Registry Catalog\necr_repositories = {{\n{new_entry}\n}}\n")
                  print(f"✅ Created new ecr_repositories block with {name}.")

          except Exception as e:
              print(f"❌ Error updating tfvars: {e}")
              sys.exit(1)
          '

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Configure AWS credentials (for Plan)
        uses: aws-actions/configure-aws-credentials@v4.1.0
        with:
          aws-region: eu-west-2
          role-to-assume: ${{ secrets.TF_AWS_IAM_ROLE_DEV }}

      - name: Terraform Init
        run: |
          terraform -chdir=envs/${{ inputs.environment }} init -backend=false

      - name: Inline Terraform Plan (Targeted)
        id: plan
        run: |
          echo "📝 Running targeted plan for module.ecr_repositories[\"${{ inputs.registry_name }}\"]..."
          
          # We use -refresh=false because we are running with -backend=false
          # This prevents Terraform from trying to reach out to a state file that isnt loaded.
          
          terraform -chdir=envs/${{ inputs.environment }} plan \
            -target="module.ecr_repositories[\"${{ inputs.registry_name }}\"]" \
            -lock=false \
            -refresh=false \
            -no-color > plan_output.txt 2>&1 || {
              echo "❌ Plan failed with error:"
              cat plan_output.txt
            }
          
          # Clean up for PR body (Markdown wrapper)
          echo "PLAN_BODY<<EOF" >> $GITHUB_ENV
          echo "<details><summary>📄 <strong>Targeted Terraform Plan</strong></summary>" >> $GITHUB_ENV
          echo "" >> $GITHUB_ENV
          echo "This plan was generated using a **bounded context** strategy (\`-target\`), focusing only on the new ECR repository." >> $GITHUB_ENV
          echo "Note: state refresh was skipped (\`-refresh=false\`) because this is a shallow plan." >> $GITHUB_ENV
          echo "" >> $GITHUB_ENV
          echo "\`\`\`hcl" >> $GITHUB_ENV
          
          # If the output is too long, we keep the last 50 lines which are usually the plan summary
          if [ $(wc -l < plan_output.txt) -gt 200 ]; then
            echo "... [Plan output truncated for brevity] ..." >> $GITHUB_ENV
            tail -n 200 plan_output.txt >> $GITHUB_ENV
          else
            cat plan_output.txt >> $GITHUB_ENV
          fi
          
          echo "\`\`\`" >> $GITHUB_ENV
          echo "</details>" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Create PR
        uses: peter-evans/create-pull-request@v6
        with:
          base: development
          branch: "registry/create-${{ inputs.registry_name }}"
          title: "🏗️ Create ECR Registry: ${{ inputs.registry_name }} (${{ inputs.environment }})"
          commit-message: "registry: create ${{ inputs.registry_name }} in ${{ inputs.environment }}"
          body: |
            ## 🏗️ ECR Registry Creation Request

            **Registry:** `${{ inputs.registry_name }}`
            **Environment:** `${{ inputs.environment }}`
            **Owner:** `${{ inputs.owner }}`
            **Risk Level:** `${{ inputs.risk }}`
            **ID:** `${{ steps.calc.outputs.id }}`

            ---

            ## 🤖 Inline Terraform Plan
            
            ${{ env.PLAN_BODY }}

            ---

            ## 🔒 Security Controls (Auto-Applied)

            Based on risk level **`${{ inputs.risk }}`**, the following security controls will be automatically configured:

            ${{ inputs.risk == 'high' && '
            ### 🔴 High Risk Configuration
            - ✅ **Encryption:** KMS (customer-managed keys)
            - ✅ **Tag Mutability:** IMMUTABLE (tags cannot be overwritten)
            - ✅ **Image Retention:** Keep last 50 images
            - ✅ **Image Scanning:** Enhanced scanning on every push
            ' || inputs.risk == 'medium' && '
            ### 🟡 Medium Risk Configuration
            - ✅ **Encryption:** AES256 (AWS-managed)
            - ✅ **Tag Mutability:** MUTABLE (tags can be overwritten)
            - ✅ **Image Retention:** Keep last 30 images
            - ✅ **Image Scanning:** Standard scanning on every push
            ' || '
            ### 🟢 Low Risk Configuration
            - ✅ **Encryption:** AES256 (AWS-managed)
            - ✅ **Tag Mutability:** MUTABLE (tags can be overwritten)
            - ✅ **Image Retention:** Keep last 20 images
            - ✅ **Image Scanning:** Standard scanning on every push
            ' }}

            ---

            ## 📋 Changes Policy
            - ✅ Added to registry catalog (`docs/catalogs/ecr-catalog.yaml`)
            - ✅ Updated documentation catalog ([REGISTRY_CATALOG.md](docs/REGISTRY_CATALOG.md))
            - ✅ Added to Terraform tfvars (`envs/${{ inputs.environment }}/terraform.tfvars`)

            ---
            
            ## 🚀 Next Steps
            1. **Merge** this PR (Plan is already verified above).
            2. **Run Apply** workflow (Automated next step).

            /cc @platform-team
          labels: |
            ecr-registry
            platform-team
            ${{ inputs.environment }}
