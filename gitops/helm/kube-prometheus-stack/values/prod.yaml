# Managed by scripts/standardize-metadata.py
prometheus:
  prometheusSpec:
    retention: 30d
    storageSpec:
      volumeClaimTemplate:
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 100Gi
alertmanager:
  alertmanagerSpec:
    storage:
      volumeClaimTemplate:
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 20Gi
grafana:
  persistence:
    enabled: true
    size: 20Gi
  additionalDataSources:
    - name: Loki
      type: loki
      access: proxy
      url: http://loki-gateway.monitoring.svc:3100
governance:
  id: HELM_KUBE_PROMETHEUS_STACK
  owner: platform-team
  risk_profile:
    production_impact: medium
    security_risk: data-visibility
    coupling_risk: medium
  reliability:
    rollback_strategy: helm-rollback
    observability_tier: gold
