# Local Environment Kustomization
# Purpose: Static secrets for local Kind cluster development
# Usage: kubectl apply -k gitops/kustomize/overlays/local
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - namespaces.yaml
  - secrets.yaml
