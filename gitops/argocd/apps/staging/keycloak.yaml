# Managed by scripts/standardize_metadata.py
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: staging-keycloak
  namespace: argocd
  annotations:
    goldenpath.idp/id: APPS_STAGING_METADATA
    goldenpath.idp/owner: None
    goldenpath.idp/risk: high
spec:
  project: default
  destination:
    namespace: keycloak
    server: https://kubernetes.default.svc
  sources:
    - repoURL: https://charts.bitnami.com/bitnami
      chart: keycloak
      targetRevision: 22.1.6
      helm:
        valueFiles:
          - $values/gitops/helm/keycloak/values/staging.yaml
    - repoURL: https://github.com/mikeybeezy/goldenpath-idp-infra.git
      targetRevision: main
      ref: values
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
