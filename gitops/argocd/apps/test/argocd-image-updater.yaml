# Argo CD Image Updater - Test Environment
# Auto-updates image tags from ECR to Git
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-argocd-image-updater
  namespace: argocd
  annotations:
    goldenpath.idp/id: APPS_TEST_ARGOCD_IMAGE_UPDATER
    goldenpath.idp/owner: platform-team
    goldenpath.idp/risk: medium
  labels:
    app.kubernetes.io/part-of: goldenpath-idp
    environment: test
spec:
  project: default
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  sources:
    - repoURL: https://argoproj.github.io/argo-helm
      chart: argocd-image-updater
      targetRevision: 0.9.1
      helm:
        valueFiles:
          - $values/gitops/helm/argocd-image-updater/values/test.yaml
    - repoURL: https://github.com/mikeybeezy/goldenpath-idp-infra.git
      targetRevision: main
      ref: values
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
